volumes:
  bucket:

networks:
  dokploy-network:
    external: true

services:
  file-sharer:
    image: ghcr.io/ricdotnet/file-sharer:0.0.5
    container_name: file-sharer
    restart: unless-stopped
    volumes:
      - bucket:/file-sharer/uploads
    networks:
      - dokploy-network
